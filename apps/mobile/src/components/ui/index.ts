// Brand primitives (existing screens import from "@/components/ui").
export { Badge, Button, Card, Field, Input, Spinner } from "./brand";

// react-native-reusables components are imported per-file, shadcn-style:
//   import { Button } from "@/components/ui/button";
//   import { Text } from "@/components/ui/text";
